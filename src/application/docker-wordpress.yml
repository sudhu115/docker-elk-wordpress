wordpress:
 build: wordpress/
 links:
   - db:mysql
 ports:
   - 8081:80
 log_driver: gelf
 log_opt:
   gelf-address: "udp://172.17.0.6:12201"
# volumes:
#  - /var/log/docker:/var/log/apache2

db:
 build: mysql/
 environment:
   MYSQL_ROOT_PASSWORD: example
